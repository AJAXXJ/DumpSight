分析以下 DPDK 崩溃场景：

客户端信息：
{{ client_info }}

DPDK 信息：
{{ dpdk_info }}

崩溃 core gdb 分析信息：
{{ core_info }}

{% if similar_cases %}
相似历史案例：
{% for case in similar_cases %}
- 案例 ID: {{ case.id }} | 摘要: {{ case.summary }}
{% endfor %}
{% endif %}

{% if few_shots %}
参考示例：
{% for shot in few_shots %}
输入：{{ shot.input }}
输出：{{ shot.output }}
{% endfor %}
{% endif %}

请分析 DPDK 崩溃原因，并严格以 JSON 格式输出。

必须遵循以下 JSON Schema：

{
  "root_cause": "string",
  "call_chain": ["string"],
  "confidence": "high | medium | low",
  "repair_steps": ["string"]
}

要求：
- 只输出 JSON，不要输出任何额外文本
- 所有字段必须存在
- repair_steps 和 call_chain 必须是数组
- confidence 只能是 high / medium / low