分析以下 DPDK 崩溃场景：

客户端信息：
{{ client_info }}

DPDK 信息：
{{ dpdk_info }}

{% if core_info %}
崩溃 core gdb 分析信息：
{{ core_info }}
{% endif %}

{% if anomaly_flags and anomaly_flags | length > 0 %}
异常标志：{{ anomaly_flags | join("，") }}
{% else %}
无异常标注
{% endif %}

{% if log_feature %}
崩溃前日志分析信息:
{{ log_feature }}
{% endif %}

{% if reference_feature %}
平均基线日志分析信息:
{{ reference_feature }}
{% endif %}

{% if similar_cases %}
相似历史案例：
{% for case in similar_cases %}
案例 ID: {{ case.case_id }}

相似度 score: {{ case.score }}

案例描述: {{ case.description }}

案例根因: {{ case.root_cause }}

修复步骤: {{ case.repair_steps }}
{% endfor %}
{% endif %}

{% if few_shots %}
参考示例：
{% for shot in few_shots %}
输入：{{ shot.input }}
输出：{{ shot.output }}
{% endfor %}
{% endif %}

请分析 DPDK 崩溃原因，并严格以 JSON 格式输出。

必须遵循以下 JSON Schema：

{
  "root_cause": "string",
  "call_chain": ["string"],
  "confidence": "high | medium | low",
  "repair_steps": ["string"]
}

要求：
- 只输出 JSON，不要输出任何额外文本
- 所有字段必须存在
- repair_steps 和 call_chain 必须是数组
- confidence 只能是 high / medium / low
- 必须使用中文输出