
你的任务是根据异常检测结果和系统当前状态，生成清晰、准确、可操作的告警通知。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【告警基本信息】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

客户端:     {{ client_id }}
告警等级:   {{ severity | upper }}
触发规则:   {{ anomaly_summary }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【当前系统状态】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

流量:
  RX:         {{ log_feature.traffic.rx_pps | round(2) }} pps
  TX:         {{ log_feature.traffic.tx_pps | round(2) }} pps
  趋势:       {{ log_feature.traffic.trend }}
  稳定性:     {{ log_feature.traffic.stability }}

错误:
  RX错误:     {{ log_feature.errors.rx_errors }}
  TX错误:     {{ log_feature.errors.tx_errors }}
  MBUF不足:   {{ log_feature.errors.nombuf }}
  端口丢包:   {{ log_feature.errors.missed }}

内存:
  MBUF池空闲: {{ (log_feature.mempool.free_ratio * 100) | round(1) }}%  [{{ log_feature.mempool.status }}]
  堆内存空闲: {{ (log_feature.heap.free_ratio * 100) | round(1) }}%    [{{ log_feature.heap.status }}]
  堆碎片率:   {{ (log_feature.heap.fragmentation * 100) | round(1) }}%

CPU:
  平均利用率: {{ (log_feature.cpu.avg_usage * 100) | round(2) }}%  [{{ log_feature.cpu.status }}]
  趋势:       {{ log_feature.cpu.trend }}
  最热核心:   {{ log_feature.cpu.hot_lcore if log_feature.cpu.hot_lcore is not none else "N/A" }}

队列:
  均衡状态:   {{ log_feature.queue.status }}
  倾斜比:     {{ log_feature.queue.imbalance_ratio | round(4) }}

综合风险:
  风险评分:   {{ log_feature.risk_score }} / 100
  风险等级:   {{ log_feature.risk_level }}
  系统洞察:   {{ log_feature.insight | join(" | ") }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【生成要求】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请生成一条运维告警通知，要求：

标题（TITLE）：
  - 不超过 30 字
  - 包含告警等级、核心异常类型、客户端标识
  - 格式示例：[CRITICAL] client_001 MBUF内存池即将耗尽

描述（DESCRIPTION）：
  - 200字以内
  - 第一段：当前异常现象（用数值说明，不要泛泛而谈）
  - 第二段：可能的根因分析（结合触发规则和系统状态）
  - 第三段：建议的处置步骤（具体可操作，优先级从高到低）
  - 不要输出 Markdown 格式，使用纯文本

请严格按以下格式输出，不要输出任何其他内容：

TITLE: <告警标题>
DESCRIPTION: <告警描述>
